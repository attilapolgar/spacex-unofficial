import {} from '../constants/action-types'

const initialState = {
  round: 1
}

export default function gameReducer(state = initialState, action) {
  switch (action.type) {
    default:
      return state
  }
}
